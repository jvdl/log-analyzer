// max size of a chunk of data to parse at once, in number of lines
export const CHUNK_SIZE = 1000;

// 1 MB in in bytes
export const ONE_MB = 1_000_000;

// max accepted log file size in bytes
export const MAX_FILE_SIZE = 10 * ONE_MB;
